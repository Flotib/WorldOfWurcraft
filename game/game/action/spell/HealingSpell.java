package game.action.spell;

import engine.texture.Texture;

public class HealingSpell extends Spell {

	public HealingSpell(Texture icon, int cost, CostType costType) {
		super(icon, cost, costType);
	}
	
}